//! Storage backend implementations (arena, memory, redb, fjall).
//!
//! Placeholder module: the backend subtree was removed at the start of the
//! store rewrite and is being rebuilt against the new `structural` traits.
